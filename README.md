# Integer Calculator in C

Simple console calculator written in C.

The program works only with integers and does not use `printf` or `scanf`.
All input and output are implemented using `read` and `write`.

## Build
```bash
gcc main.c -Wall -Wextra -Werror -o calc
```
## Run
```bash
./calc
```
